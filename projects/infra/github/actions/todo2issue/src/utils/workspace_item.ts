/**
 * Converts the relative filename to the absolute
 * filename by prefixing the workspace location.
 * 
 * @param filename The relative filename.
 */
export const getWorkspaceItem = (filename: string): string => {
  return "";
};
